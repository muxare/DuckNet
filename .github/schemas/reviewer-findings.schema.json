{
  "type": "object",
  "definitions": {
    "finding": {
      "type": "object",
      "properties": {
        "reviewer": {
          "type": "string",
          "enum": ["architecture", "security"]
        },
        "severity": {
          "type": "string",
          "enum": ["critical", "major", "minor"]
        },
        "file": { "type": "string" },
        "detail": { "type": "string" },
        "confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "rule": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5
        }
      },
      "required": ["severity", "detail"],
      "additionalProperties": false
    }
  },
  "properties": {
    "reviewer": {
      "type": "string",
      "enum": ["architecture", "security"]
    },
    "findings": {
      "type": "array",
      "items": { "$ref": "#/definitions/finding" }
    },
    "notes": {
      "type": "array",
      "items": { "type": "string" }
    },
    "error": { "type": "string" }
  },
  "required": ["reviewer", "findings", "notes"],
  "additionalProperties": false
}
